package RevisaoEstruturaCondicional;

import javax.swing.JOptionPane;

public class OrdenacaoInteiros {

	public static void main(String[] args) {
		int X = 0,Y = 0,R = 0;
		
		X = Integer.parseInt(JOptionPane.showInputDialog(null,"Digite um valor positivo: "));
		Y = Integer.parseInt(JOptionPane.showInputDialog(null,"Digite um valor positivo: "));
		
		if(X > 0 && Y > 0 ) {
			if(X > Y ) {
				
			}
		}

	}

}
